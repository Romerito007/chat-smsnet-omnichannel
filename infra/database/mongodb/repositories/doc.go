// Package repositories is the root for the Mongo implementations of the domain
// repository interfaces. Each domain gets its own subpackage
// (infra/database/mongodb/repositories/<domain>) implementing the interface
// declared in domain/<domain>/repository. Empty in the bootstrap.
package repositories
